let bootcamp = "web dev";
let student = "marcos";

let duration = 24;

// concatenation

duration += 3; // duration = duration + 3

console.log(duration); // 27

duration = "24";
duration += 3;

console.log(duration); // 243

console.log(typeof price); // number

console.log(`${bootcamp} is ${duration} weeks long`); // web dev is 24 weeks long

console.log(bootcamp + " is " + duration + " weeks long"); // web dev is 24 weeks long

console.log(`${student} is ${22 + 10} years old`); // marcos is 32 years old

// length is not a method, it is a property so it doesn't have () in the end
console.log(`How long is this string: ${str.length}`); // 23
